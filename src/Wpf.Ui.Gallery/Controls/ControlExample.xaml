<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:helpers="clr-namespace:Wpf.Ui.Gallery.Helpers"
    xmlns:syntax="http://schemas.lepo.co/wpfui/2022/xaml/syntax"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    <ControlTemplate
        x:Key="ControlExample.DefaultTemplate"
        TargetType="{x:Type controls:ControlExample}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock
                Grid.Row="0"
                Margin="0,0,0,10"
                FontTypography="BodyStrong"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="{TemplateBinding HeaderText}"
                Visibility="{TemplateBinding HeaderText,
                                                     Converter={StaticResource NullToVisibilityConverter}}" />

            <Border
                Grid.Row="1"
                Padding="16"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1,1,1,0"
                CornerRadius="8,8,0,0">
                <ContentPresenter Content="{TemplateBinding ExampleContent}" />
            </Border>

            <ui:CardExpander
                Grid.Row="2"
                CornerRadius="0,0,8,8"
                Header="{TemplateBinding ExpanderHeader}">
                <ContentPresenter Content="{TemplateBinding ExpanderContent}" />
            </ui:CardExpander>
        </Grid>
    </ControlTemplate>

    <ControlTemplate
        x:Key="ControlExample.SimpleTemplate"
        TargetType="{x:Type controls:ControlExample}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock
                Grid.Row="0"
                Margin="0,0,0,10"
                FontTypography="BodyStrong"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="{TemplateBinding HeaderText}"
                Visibility="{TemplateBinding HeaderText,
                                                     Converter={StaticResource NullToVisibilityConverter}}" />

            <Border
                Grid.Row="1"
                Padding="16"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <ContentPresenter Content="{TemplateBinding ExampleContent}" />
            </Border>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type controls:ControlExample}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource ControlExample.DefaultTemplate}" />
        <Style.Triggers>
            <Trigger Property="IsExpanderVisible" Value="False">
                <Setter Property="Template" Value="{StaticResource ControlExample.SimpleTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
