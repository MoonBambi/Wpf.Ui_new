<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:basicInput="clr-namespace:Wpf.Ui.Gallery.Views.Pages.BasicInput"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:helpers="clr-namespace:Wpf.Ui.Gallery.Helpers"
    xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <helpers:TerminalToCardBackgroundConverter x:Key="TerminalToCardBackgroundConverter" />

    <Style TargetType="{x:Type controls:GalleryNavigationPresenter}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:GalleryNavigationPresenter}">
                    <ItemsControl ItemsSource="{TemplateBinding ItemsSource}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:NavigationCard}">
                                <ui:CardAction
                                    Width="240"
                                    Height="140"
                                    Margin="6"
                                    Padding="14,16,14,4"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Stretch"
                                    IsChevronVisible="False"
                                    Background="{Binding Description, Converter={StaticResource TerminalToCardBackgroundConverter}}">
                                    <Grid Margin="16,8,16,8">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="60" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>

                                        <StackPanel
                                            Grid.Row="0"
                                            Orientation="Vertical">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <CheckBox
                                                    Grid.Column="0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top"
                                                    Margin="-2,-6,4,0"
                                                    Width="18"
                                                    Height="18"
                                                    Padding="0"
                                                    MinWidth="0"
                                                    Background="#F7F9FC"
                                                    BorderBrush="{DynamicResource CheckBoxBorderBrush}"
                                                    BorderThickness="2">
                                                    <CheckBox.Template>
                                                        <ControlTemplate TargetType="CheckBox">
                                                            <Border
                                                                x:Name="Border"
                                                                Width="18"
                                                                Height="18"
                                                                CornerRadius="9"
                                                                Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                                <Path
                                                                    x:Name="CheckMark"
                                                                    Data="M 4 9 L 7 12 L 13 5"
                                                                    Stroke="{DynamicResource CheckBoxCheckGlyphForeground}"
                                                                    StrokeThickness="2"
                                                                    StrokeStartLineCap="Round"
                                                                    StrokeEndLineCap="Round"
                                                                    Visibility="Collapsed" />
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource CheckBoxCheckBackgroundFillChecked}" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </CheckBox.Template>
                                                </CheckBox>

                                                <ui:TextBlock
                                                    Grid.Column="1"
                                                    FontTypography="BodyStrong"
                                                    Appearance="Secondary"
                                                    Text="{Binding Name}"
                                                    TextWrapping="Wrap" />
                                            </Grid>

                                            <ui:TextBlock
                                                Margin="0,4,0,0"
                                                FontTypography="Caption"
                                                Appearance="Tertiary"
                                                Text="{Binding Description}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>

                                        <Border
                                            Grid.Row="1"
                                            Margin="0,12,0,4"
                                            Height="1"
                                            Background="{DynamicResource ControlElevationBorderBrush}" />

                                        <StackPanel
                                            Grid.Row="2"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Margin="0,8,0,0">
                                            <ui:Button
                                                Width="36"
                                                Height="36"
                                                Padding="0"
                                                Margin="0,0,8,0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Appearance="Transparent"
                                                Command="{Binding Path=EditButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:GalleryNavigationPresenter}}}"
                                                CommandParameter="{Binding}"
                                                Icon="{ui:SymbolIcon Edit24, FontSize=20}" />
                                            <ui:Button
                                                Width="36"
                                                Height="36"
                                                Padding="0"
                                                Margin="0,0,8,0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Appearance="Transparent"
                                                Command="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:GalleryNavigationPresenter}}}"
                                                CommandParameter="{Binding}"
                                                Icon="{ui:SymbolIcon Play24, FontSize=20}" />
                                            <ui:Button
                                                Width="36"
                                                Height="36"
                                                Padding="0"
                                                Margin="0,0,0,0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Appearance="Transparent"
                                                Command="{Binding Path=DeleteButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type controls:GalleryNavigationPresenter}}}"
                                                CommandParameter="{Binding}"
                                                Icon="{ui:SymbolIcon Delete24, FontSize=20}" />
                                        </StackPanel>
                                    </Grid>
                                </ui:CardAction>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
