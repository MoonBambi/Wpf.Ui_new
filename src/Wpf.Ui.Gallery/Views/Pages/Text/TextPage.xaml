<Page
    x:Class="Wpf.Ui.Gallery.Views.Pages.Text.TextPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Gallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Wpf.Ui.Gallery.Views.Pages.Text"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:Wpf.Ui.Gallery.Models"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:effects="clr-namespace:Wpf.Ui.Gallery.Effects"
    Title="TextPage"
    controls:PageControlDocumentation.Show="False"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Key="TerminalExpandStoryboard">
            <effects:GridLengthAnimation
                Duration="0:0:0.25"
                Storyboard.TargetName="TerminalRow"
                Storyboard.TargetProperty="Height" />
        </Storyboard>
        <Storyboard x:Key="TerminalCollapseStoryboard">
            <effects:GridLengthAnimation
                Duration="0:0:0.25"
                Storyboard.TargetName="TerminalRow"
                Storyboard.TargetProperty="Height" />
        </Storyboard>

        <SolidColorBrush x:Key="CommandBrush" Color="#DCDCAA" />
        <SolidColorBrush x:Key="OutputBrush" Color="#D4D4D4" />
        <SolidColorBrush x:Key="ErrorBrush" Color="#FF6B68" />
        <SolidColorBrush x:Key="SystemBrush" Color="#569CD6" />

        <SolidColorBrush x:Key="CommandBrushLight" Color="#0057D2" />
        <SolidColorBrush x:Key="OutputBrushLight" Color="#707070" />
        <SolidColorBrush x:Key="ErrorBrushLight" Color="#C05050" />
        <SolidColorBrush x:Key="SystemBrushLight" Color="#188038" />
        <SolidColorBrush x:Key="TerminalBackgroundLight" Color="#F7F9FB" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition x:Name="TerminalRow" Height="*" />
        </Grid.RowDefinitions>

        <ScrollViewer
            x:Name="ContentScrollViewer"
            Grid.Row="0"
            VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <StackPanel Margin="10">
                        <TextBlock
                            x:Name="MainTitle"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="28"
                            FontWeight="Black" />
                    </StackPanel>
                </Grid>

                <controls:GalleryNavigationPresenter
                    x:Name="CommandsPresenter"
                    Grid.Row="1"
                    Margin="0, 10"
                    PreviewMouseLeftButtonUp="CommandsPresenter_OnPreviewMouseLeftButtonUp"
                    ItemsSource="{Binding ViewModel.NavigationCards, Mode=OneWay}" />
            </Grid>
        </ScrollViewer>

        <ui:Card
            Grid.Row="0"
            Margin="-40,0,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Width="32"
            Padding="0">
            <StackPanel Orientation="Vertical">
                <ui:Button
                    Width="32"
                    Height="32"
                    Margin="0,0,0,8"
                    Background="Transparent"
                    BorderThickness="0"
                    Appearance="Transparent"
                    Padding="0"
                    Click="SelectAllButton_OnClick">
                    <ui:SymbolIcon
                        x:Name="SelectAllIcon"
                        Symbol="SelectAllOn24"
                        FontSize="24" />
                </ui:Button>

                <Border
                    Height="1"
                    Margin="0,0,0,8">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#00000000" Offset="0.0" />
                            <GradientStop Color="#33000000" Offset="0.5" />
                            <GradientStop Color="#00000000" Offset="1.0" />
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <ui:Button
                    Width="32"
                    Height="32"
                    Margin="0,0,0,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Appearance="Transparent"
                    Padding="0"
                    x:Name="DeleteSelectedButton"
                    IsEnabled="False"
                    Click="DeleteAllButton_OnClick">
                    <ui:SymbolIcon
                        x:Name="DeleteSelectedIcon"
                        Symbol="DeleteOff24"
                        FontSize="24" />
                </ui:Button>
            </StackPanel>
        </ui:Card>

        <GridSplitter
            Grid.Row="1"
            Height="4"
            HorizontalAlignment="Stretch"
            ResizeDirection="Rows"
            ResizeBehavior="PreviousAndNext"
            Background="Transparent" />

        <Border
            Grid.Row="2"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid
                        x:Name="TerminalHeaderGrid"
                        Grid.Row="0"
                        Margin="8,4,8,0"
                        SizeChanged="TerminalHeaderGrid_OnSizeChanged">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            VerticalAlignment="Center"
                            Text="输出" />

                        <StackPanel
                            Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                            <ui:Button
                                x:Name="TerminalToggleButton"
                                Margin="0,0,8,0"
                                Icon="{ui:SymbolIcon ChevronUp24}"
                                Click="ToggleTerminalButton_OnClick" />
                            <ui:Button
                                x:Name="TerminalClearButton"
                                Icon="{ui:SymbolIcon Eraser24}"
                                Click="TerminalClearButton_OnClick" />
                        </StackPanel>
                    </Grid>

                    <!-- 卡片内部的分隔线 -->
                    <Border
                        Grid.Row="1"
                        Margin="0,4,0,0"
                        BorderThickness="0,1,0,0"
                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" />

                    <RichTextBox
                        x:Name="TerminalRichTextBox"
                        Grid.Row="2"
                        Margin="4,4,4,4"
                        IsReadOnly="True"
                        BorderThickness="0"
                        Background="Transparent"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        FontFamily="Cascadia Code SemiBold"
                        FontSize="16"
                        FontWeight="Bold"
                        TextChanged="TerminalRichTextBox_OnTextChanged" />
                </Grid>
            </Border>
    </Grid>
</Page>
